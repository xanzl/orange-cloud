# 自编译 OAuth 回调中转（Cloudflare Workers 单文件版）
# 用法：cd relay && npx wrangler deploy
name = "orange-cloud-oauth-relay"
main = "src/index.js"
compatibility_date = "2026-01-01"

# 自定义域（需 xanz.top 在这个 Cloudflare 账号下）
routes = [
  { pattern = "oc-api.xanz.top", custom_domain = true },
]